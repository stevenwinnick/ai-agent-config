---
name: read-jira-ticket
description: Read a Jira ticket to understand its contents and requirements
argument-hint: <ticket-id>
---

# Read Jira Ticket

Use the Atlassian MCP to read the specified Jira ticket.

**Ticket ID:** $ARGUMENTS

Use the Atlassian MCP to fetch the ticket details (the tool may be named `get_jira_issue` or `getJiraIssue` depending on the agent)
